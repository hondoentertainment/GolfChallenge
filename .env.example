# Copy to .env.local for development. Set these in Vercel → Project → Settings → Environment Variables for production.

# Required: Neon (or compatible) Postgres connection string
DATABASE_URL=

# Required in production: strong secret for signing session JWTs (never commit the real value)
JWT_SECRET=

# Optional: first admin user (lowercase email must match registered user)
ADMIN_EMAIL=

# Optional: comma-separated emails allowed on /admin/metrics-review (default: hondo4185@gmail.com; must be is_admin)
METRICS_REVIEW_ALLOWED_EMAILS=

# Optional: GitHub PAT for higher rate limits when loading CI status on metrics review page
GITHUB_TOKEN=

# Required on Vercel production for cron routes (Authorization: Bearer <CRON_SECRET>)
CRON_SECRET=

# Optional: Resend for transactional email
RESEND_API_KEY=
FROM_EMAIL=

# Optional: canonical site URL (invites, email links)
NEXT_PUBLIC_APP_URL=https://your-deployment.vercel.app
