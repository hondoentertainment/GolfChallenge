# Local scripts

Machine-specific or one-off scripts belong here. Git ignores everything under `scripts/local/` except `.gitkeep` and this file (see the repo root `.gitignore`).

When you add a helper script locally, run it with `node scripts/local/your-script.mjs` from the project root.
